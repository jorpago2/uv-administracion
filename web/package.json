{
  "name": "guia-operativa-uv",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.16.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:search": "node ../scripts/generar_indice_busqueda_web.mjs",
    "prebuild": "pnpm generate:search",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
